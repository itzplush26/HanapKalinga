appId: ${APP_ID}
---
# Login as admin user
- runFlow:
    file: ../shared/setup.yaml
- runFlow:
    file: ../shared/helpers.yaml
    env:
      TEST_EMAIL: ${TEST_EMAIL}
      TEST_PASSWORD: ${TEST_PASSWORD}

# Navigate to Verifications tab
- tapOn:
    id: "tab_verifications"

# Wait for queue
- extendedWaitUntil:
    visible:
      id: "verificationQueue_list_requests"
    timeout: 15000

# Scroll to and tap on verification request
- scrollUntilVisible:
    element:
      id: "verificationQueue_card_${VERIFICATION_ID}"
    direction: DOWN
    timeout: 15000
    speed: 40
- tapOn:
    id: "verificationQueue_card_${VERIFICATION_ID}"

# Wait for detail screen
- extendedWaitUntil:
    visible:
      id: "verificationDetail_text_name"
    timeout: 10000

# Scroll to and tap approve
- scrollUntilVisible:
    element:
      id: "verificationDetail_button_approve"
    direction: DOWN
    timeout: 10000
    speed: 40
- tapOn:
    id: "verificationDetail_button_approve"

# Wait for action to complete and return to queue
- extendedWaitUntil:
    visible:
      id: "verificationQueue_list_requests"
    timeout: 15000
