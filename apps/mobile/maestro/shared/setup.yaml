appId: ${APP_ID}
env:
  APP_ID: ${APP_ID}
---
# Launch the app and wait for the landing screen
- launchApp:
    appId: ${APP_ID}
    stopApp: true
    clearState: true
    timeout: 30000

# Wait for the landing screen to render
- assertVisible:
    id: "landing_button_login"
    timeout: 15000
