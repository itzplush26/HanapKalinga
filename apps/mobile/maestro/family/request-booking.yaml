appId: ${APP_ID}
---
# Login as family user
- runFlow:
    file: ../shared/setup.yaml
- runFlow:
    file: ../shared/helpers.yaml
    env:
      TEST_EMAIL: ${TEST_EMAIL}
      TEST_PASSWORD: ${TEST_PASSWORD}

# Navigate to Browse tab
- extendedWaitUntil:
    visible:
      text: "Browse"
    timeout: 15000
- tapOn:
    text: "Browse"

# Wait for browse screen to load
- extendedWaitUntil:
    visible:
      id: "browse_input_search"
    timeout: 10000

# Wait for nurse list
- extendedWaitUntil:
    visible:
      id: "browse_list_nurses"
    timeout: 15000

# Tap on nurse card
- tapOn:
    id: "browse_card_nurse_${NURSE_ID}"

# Verify nurse detail screen loaded
- extendedWaitUntil:
    visible:
      id: "nurseDetail_text_name"
    timeout: 10000

# Tap request booking
- tapOn:
    id: "nurseDetail_button_requestBooking"

# Wait for new booking screen
- extendedWaitUntil:
    visible:
      id: "bookingNew_picker_date"
    timeout: 10000

# Select patient condition
- tapOn:
    id: "bookingNew_input_condition_bedridden"

# Select shift
- tapOn:
    id: "bookingNew_picker_shift_morning"

# Select a required skill (tap by testID)
- scrollUntilVisible:
    element:
      id: "bookingNew_chip_skill_Wound_Care"
    direction: DOWN
    timeout: 5000
- tapOn:
    id: "bookingNew_chip_skill_Wound_Care"

# Select budget
- scrollUntilVisible:
    element:
      id: "bookingNew_input_budget_under_1000"
    direction: DOWN
    timeout: 5000
- tapOn:
    id: "bookingNew_input_budget_under_1000"

# Submit booking request
- scrollUntilVisible:
    element:
      id: "bookingNew_button_submit"
    direction: DOWN
    timeout: 5000
- tapOn:
    id: "bookingNew_button_submit"

# Wait for booking success modal
- extendedWaitUntil:
    visible:
      id: "bookingNew_text_success"
    timeout: 15000
